<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Income Overview</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .navbar {
            background-color: #007BFF;
            color: white;
            padding: 15px;
            text-align: center;
        }
        .navbar a {
            color: white;
            margin: 0 15px;
            text-decoration: none;
        }
        .container {
            width: 80%;
            margin: 20px auto;
        }
        .message {
            color: #555;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th, td {
            padding: 10px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .btn {
            padding: 10px 15px;
            background-color: #007BFF;
            border: none;
            color: white;
            cursor: pointer;
        }
        .btn:hover {
            background-color: #0056b3;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .form-group input {
            padding: 10px;
            width: 100%;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <a href="/home">Home</a>
        <a href="/logout">Logout</a>
    </div>

    <div class="container">
        
        <h2>Categorie wajib</h2>
        <form action="/categories-setup/create" method="POST">
            <h2>Apakah Anda ingin menambahkan kategori untuk pengeluaran harian?</h2>
            
            <!-- SARAPAN -->
            <label for="sarapan">Apakah Anda orang yang sarapan?</label>
            <input type="checkbox" id="sarapan" name="sarapan" value="true" onchange="toggleBudgetInput('sarapan')"><br>
            <!-- HIDDEN -->
            <div id="sarapanBudget" style="display:none;">
                <label for="sarapanBudgetInput">Masukkan budget untuk sarapan:</label>
                <input type="number" id="sarapanBudgetInput" name="sarapan_budget" placeholder="Budget Sarapan">
            </div>
            
            <!-- MAKAN SIANG -->
            <label for="makansiang">Apakah Anda ingin menambahkan kategori makan siang?</label>
            <input type="checkbox" id="makansiang" name="makansiang" value="true" onchange="toggleBudgetInput('makansiang')"><br>
            <!-- HIDDEN -->
            <div id="makansiangBudget" style="display:none;">
                <label for="makansiangBudgetInput">Masukkan budget untuk makan siang:</label>
                <input type="number" id="makansiangBudgetInput" name="makansiang_budget" placeholder="Budget Makan Siang">
            </div>
            
            <!-- MAKAN MALAM -->
            <label for="makanmalam">Apakah Anda ingin menambahkan kategori makan malam?</label>
            <input type="checkbox" id="makanmalam" name="makanmalam" value="true" onchange="toggleBudgetInput('makanmalam')"><br>
            <!-- HIDDEN -->
            <div id="makanmalamBudget" style="display:none;">
                <label for="makanmalamBudgetInput">Masukkan budget untuk makan malam:</label>
                <input type="number" id="makanmalamBudgetInput" name="makanmalam_budget" placeholder="Budget Makan Malam">
            </div>
        
            <button type="submit">Buat Kategori</button>
        </form>
    </div>

    <script>
        function toggleBudgetInput(category) {
            const checkbox = document.getElementById(category);
            const budgetInput = document.getElementById(category + 'Budget');

            if (checkbox.checked) {
                budgetInput.style.display = 'block';
            } else {
                budgetInput.style.display = 'none';
            }
        }
    </script>
</body>
</html>
